


我想开发一个自用的导航站点,用来导航我的家庭服务器上的 docker web 服务

通过连接 docker接口自动扫描,检查带有指定标签的服务,自动生成导航

通过标签可以配置:

 - 名字,必填
 - 域名/ip - 选填,默认只是 docker 服务所在服务器的域名/ip
 - 端口,选填,默认80
 - 图标 - 选填

自动检测 图标 的 url 是否更改, 如果更新了就下载到服务本地,不要使用远程的图标
